syntax = "proto3";

// Goコードのパッケージとして使用される
package log.v1;

option go_package = "github.com/yamane/api/log_v1";

// スライスを定義したい場合はrepeatedキーワードを使用する
// (protoBuf) repeated Record records = (Go) records []Record
message Record {
  bytes value = 1;
  uint64 offset = 2;
}